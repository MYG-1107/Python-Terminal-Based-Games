const filters=document.querySelectorAll(".filter");const cards=document.querySelectorAll(".game-card");const empty=document.getElementById("emptyState");document.getElementById("gameCount").textContent=cards.length;filters.forEach(button=>button.addEventListener("click",()=>{filters.forEach(b=>b.classList.remove("active"));button.classList.add("active");const filter=button.dataset.filter;let visible=0;cards.forEach(card=>{const show=filter==="all"||card.dataset.category.split(" ").includes(filter);card.hidden=!show;if(show)visible++});empty.hidden=visible!==0;}));console.log("%cPython Terminal Games Arcade","color:#52f5a7;font:700 18px monospace");